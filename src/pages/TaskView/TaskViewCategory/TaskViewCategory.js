import React from "react";

export default function TaskViewCategory({
    key,
    category,
    index,
}) {
    const categoryItems = category.items.map((e, i) => (<

    />))

    return (
        <div>213132</div>
    )
}